{
  "name": "flapjack",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "watch": "nodemon --watch ./models --watch *.js --ignore dist/ --exec npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "deploy": "rimraf app.yaml && node appYamlGenerator.js && gcloud app deploy",
    "start": "npm run build && node dist/server.js",
    "ds": "docker run -it -p 8000:8000 --rm --env-file ./variables.env flapjack-api-dev"
  },
  "quokka": {
    "babel": true
  },
  "engines": {
    "node": "8.x"
  },
  "author": "Conor O'Flanagan",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "bcrypt": "^2.0.1",
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "express-graphql": "^0.6.12",
    "graphql": "^0.13.2",
    "jquery": "^3.3.1",
    "jsdom": "^11.11.0",
    "jsonwebtoken": "^8.2.2",
    "lodash": "^4.17.10",
    "mongoose": "^5.1.3",
    "morgan": "^1.9.0",
    "passport": "^0.4.0",
    "passport-jwt": "^4.0.0",
    "passport-local": "^1.0.0",
    "request": "^2.87.0",
    "rimraf": "^2.6.2",
    "validator": "^10.3.0"
  },
  "devDependencies": {
    "chai": "*",
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.12.0",
    "mocha": "*",
    "nodemon": "^1.17.5"
  }
}
